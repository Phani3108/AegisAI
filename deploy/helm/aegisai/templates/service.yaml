apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegisai.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "aegisai.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "aegisai.name" . }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
